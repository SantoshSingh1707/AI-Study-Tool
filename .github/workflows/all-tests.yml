name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    uses: ./.github/workflows/
    with:
      run-backend: true
      run-frontend: true
      upload-coverage: true
